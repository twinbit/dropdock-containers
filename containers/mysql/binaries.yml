mysql:
  mysql_create:
    bin: "/create"
    env: "#!/usr/bin/env bash"
    arguments: "${@}"
  mysql_import:
    bin: "/import"
    env: "#!/usr/bin/env bash"
    arguments: "${@}"
  mysql_cli:
    bin: "/cli"
    env: "#!/usr/bin/env bash"
    arguments: "${@}"
